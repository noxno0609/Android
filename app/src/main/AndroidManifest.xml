<?xml version="1.0" encoding="utf-8"?>
<manifest
        xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.thienpham.time_remider">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/icon_timereminder"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name=".MainActivity">
        </activity>

        <activity
                android:name=".ShowActivity">
        </activity>

        <activity
                android:name=".SignupActivity">

        </activity>

        <activity
                android:name=".WeekViewAcitivity">

        </activity>

        <activity
                android:name=".LichtrinhActivity"
                android:theme="@style/Theme.AppCompat.Light.DialogWhenLarge">
        </activity>

        <activity
                android:name=".MocAcitivty"
                android:theme="@style/Theme.AppCompat.Light.DialogWhenLarge">
        </activity>

        <activity
                android:name=".MainLichTrinhActivity">

        </activity>
    </application>

</manifest>